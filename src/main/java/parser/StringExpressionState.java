package parser;

import lexer.input.LexerInput;
import parser.AST.ASTNode;

public class StringExpressionState implements ParserState<Parser> {

    @Override
    public ASTNode parse(Parser parser, LexerInput lexer) {
        return null;
    }
}
